import { Navigate } from 'react-router-dom';
import { BrowserRouter as Router, Routes, Route } from 'react-router-dom';

const UseNavigate = (pages) => {
 console.log({pages})
 return <navigate to={pages} />;
}

export default UseNavigate;




